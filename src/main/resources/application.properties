#spring.datasource.url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/Groupe8?sslmode=require&user=postgres&password=postgres}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/Groupe8?user=postgres&password=cjpst2613}

# regarder du coté de .env
# noms des tables pas en majuscules 

spring.thymeleaf.mode=HTML

logging.level.org.springframework=INFO

spring.profiles.active=production

server.port=${PORT:8080}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.hibernate.ddl-auto=create-drop
spring.sql.init.mode=ALWAYS
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.generate-ddl=true

server.error.include-message=always

spring.jackson.serialization.fail-on-empty-beans=false
 

 
# spring.datasource.tomcat.connection-properties=useUnicode=true;
# characterEncoding=utf-8; 
